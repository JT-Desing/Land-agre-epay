import React from "react";
import ReactDOM from "react-dom/client";
import EpaycoAgregadorLanding from "./EpaycoAgregadorLanding";
import "./styles.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <EpaycoAgregadorLanding />
  </React.StrictMode>,
);
